class Application {
    def main {
        Int x = 1;
        switch x {
            case 1:
                out.writeln("Um");
            case 2:
                out.writeln("Um");
                out.writeln("Dois");
            case default:
                out.writeln(x);
        }
    }
}