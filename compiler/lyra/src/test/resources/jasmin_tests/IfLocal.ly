class Application {
    def main {
        Bool gringo = false;
        String text = "FIM";
        if (gringo) {
            out.writeln("Hello World!");
        } else {
            String text = "Olá Mundo!";
            out.writeln(text);
        }
        out.writeln(text);
    }
}
